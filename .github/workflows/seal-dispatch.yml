name: Seal custom code (dispatch)

# When genuine out-of-band custom code is merged to this SDK repo's main,
# notify G-Core/stlc-config to reseal so the custom-code tracking JSON
# (the {base, integrated} commit pair) absorbs it. stlc-authored commits are
# skipped by the loop guards below, so the bot's own pushes (the "Build SDK"
# squash commit and the docs-regen commit) can't trigger a reseal loop.
#
# Automation pushes are skipped wholesale (guard 3): "Sync from production"
# fast-forwards this trunk with release commits (version/CHANGELOG) using a
# gcore-sdk-bot App token, and App-token pushes trigger workflows. Those
# release commits need no dispatch because the next regular build advances
# each tracking file to the live origin/main tip. Dispatching here would run
# an unnecessary multi-target reseal for every release.
#
# This workflow is itself custom code: it must be sealed once (stlc build
# --commit) to bootstrap, after which it is preserved across regenerations.
on:
  push:
    # main only. stlc preview/integrated/codegen branches never push to main,
    # so listing main here already excludes them.
    branches: [main]

concurrency:
  group: seal-dispatch-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  dispatch:
    runs-on: ubuntu-latest
    # Canonical staging repo on the upstream only (never forks).
    if: github.repository == 'G-Core/terraform-provider-gcore-staging'
    steps:
      - name: Generate gcore-sdk-bot token
        id: app-token
        uses: actions/create-github-app-token@v3
        with:
          client-id: ${{ vars.GCORE_SDK_BOT_CLIENT_ID }}
          private-key: ${{ secrets.GCORE_SDK_BOT_PRIVATE_KEY }}
          owner: G-Core
          repositories: stlc-config
          permission-contents: write

      - name: Loop-guard and send reseal dispatch
        env:
          DISPATCH_TOKEN: ${{ steps.app-token.outputs.token }}
          HEAD_MSG: ${{ github.event.head_commit.message }}
          HEAD_AUTHOR_USERNAME: ${{ github.event.head_commit.author.username }}
          HEAD_AUTHOR_NAME: ${{ github.event.head_commit.author.name }}
          HEAD_COMMITTER_EMAIL: ${{ github.event.head_commit.committer.email }}
          HEAD_COMMITTER_USERNAME: ${{ github.event.head_commit.committer.username }}
          SHA: ${{ github.sha }}
          REF: ${{ github.ref }}
          ACTOR: ${{ github.actor }}
          REPO: ${{ github.repository }}
        run: |
          set -euo pipefail

          # Loop guard 1: skip commits produced by an stlc build, which carry a
          # Stainless-Generated-From trailer. Authorship (guard 2) does not cover
          # these -- `stlc build --commit` run locally is committed with the
          # operator's own identity, not stlc-bot's.
          #
          # GitHub-UI merges are excluded: a squash merge folds every commit
          # message in the PR into its body, so a human PR carrying a generated
          # commit would match here and its custom code would never be sealed.
          # Such merges are always committed by GitHub <noreply@github.com> /
          # web-flow; an stlc build push never is.
          #
          # Anchored to a trailer line so a message that merely mentions the
          # trailer does not match.
          if [ "$HEAD_COMMITTER_EMAIL" != "noreply@github.com" ] \
             && [ "$HEAD_COMMITTER_USERNAME" != "web-flow" ] \
             && printf '%s' "$HEAD_MSG" | grep -qE '^Stainless-Generated-From:[[:space:]]'; then
            echo "Head commit is an stlc build (trailer present, not a GitHub merge) — skipping dispatch."
            exit 0
          fi

          # Loop guard 2: skip stlc-bot commits (e.g. the docs-regen commit).
          if [ "$HEAD_AUTHOR_USERNAME" = "stlc-bot" ] || [ "$HEAD_AUTHOR_NAME" = "stlc-bot" ]; then
            echo "Head commit authored by stlc-bot — skipping dispatch."
            exit 0
          fi

          # Loop guard 3: App-authenticated back-sync pushes trigger workflows,
          # unlike pushes made with GITHUB_TOKEN. They contain release metadata,
          # not out-of-band custom code that needs resealing.
          if [ "$ACTOR" = "gcore-sdk-bot[bot]" ]; then
            echo "Push performed by gcore-sdk-bot[bot] (automation, e.g. production back-sync) — skipping dispatch."
            exit 0
          fi

          # Reseal all targets (matches the commit-back scope on stlc-config).
          cat > /tmp/dispatch-payload.json <<JSON
          {
            "event_type": "seal-custom-code",
            "client_payload": {
              "target": "all",
              "sha": "${SHA}",
              "ref": "${REF}",
              "actor": "${ACTOR}",
              "repo": "${REPO}"
            }
          }
          JSON

          echo "Sending seal-custom-code dispatch to G-Core/stlc-config for ${REPO}@${SHA}."
          http_code=$(curl -sS -o /tmp/dispatch-resp.txt -w '%{http_code}' \
            -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${DISPATCH_TOKEN}" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            https://api.github.com/repos/G-Core/stlc-config/dispatches \
            -d @/tmp/dispatch-payload.json)

          if [ "$http_code" != "204" ]; then
            echo "Dispatch failed with HTTP ${http_code}:" >&2
            cat /tmp/dispatch-resp.txt >&2
            exit 1
          fi
          echo "Dispatch accepted (HTTP 204)."
