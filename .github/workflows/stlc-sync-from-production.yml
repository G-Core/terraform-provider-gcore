name: Sync from production

# Sync production to staging: fast-forward the staging trunk up to production
# `main`, bringing release commits (version/changelog) back so the next build
# reseals against the released state. Preserves commit SHAs.
on:
  schedule:
    - cron: '17 */6 * * *'
  workflow_dispatch: {}
  repository_dispatch:
    types: [prod-released]

permissions:
  contents: write

concurrency:
  group: stlc-sync-from-production
  cancel-in-progress: true

jobs:
  sync:
    runs-on: ubuntu-latest
    if: github.repository == 'G-Core/terraform-provider-gcore-staging'
    env:
      PRODUCTION_REPO: G-Core/terraform-provider-gcore
      PRODUCTION_BRANCH: v2
    steps:
      - name: Check out staging
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Fetch production main
        run: |
          # Read-only public fetch via the built-in token.
          git remote add production "https://github.com/${PRODUCTION_REPO}.git"
          git fetch production "${PRODUCTION_BRANCH}"

      - name: Check whether production has content staging lacks
        id: diff
        run: |
          MERGED=$(git merge-tree --write-tree origin/main "production/${PRODUCTION_BRANCH}") || MERGED=conflict
          STAGING_TREE=$(git rev-parse 'origin/main^{tree}')
          if [ "$MERGED" = "$STAGING_TREE" ]; then
            echo "Staging already has production's content. Nothing to pull back."
            echo "behind=false" >> "$GITHUB_OUTPUT"
          else
            echo "behind=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Sync production to staging (fast-forward)
        if: steps.diff.outputs.behind == 'true'
        run: |
          # Refuse unless staging/main is an ancestor of production/main (else
          # the staging trunk has forked from production and can't be fast-forwarded).
          if ! git merge-base --is-ancestor origin/main "production/${PRODUCTION_BRANCH}"; then
            echo "::error title=Back-sync blocked::staging main is not an ancestor of production/${PRODUCTION_BRANCH}."
            exit 1
          fi
          git push origin "production/${PRODUCTION_BRANCH}:refs/heads/main"
          echo "Pushed production/${PRODUCTION_BRANCH} to staging/main via fast-forward."
