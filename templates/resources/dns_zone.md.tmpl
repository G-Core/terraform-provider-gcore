---
page_title: "{{ .Name }} {{ .Type }} - {{ .RenderedProviderName }}"
subcategory: ""
description: |-
  {{ .Description }}
---

# {{ .Name }} ({{ .Type }})

{{ .Description }}

## Example Usage

### Basic DNS zone

Creates a DNS zone with default settings.

{{tffile "examples/resources/gcore_dns_zone/basic.tf"}}

### DNS zone with SOA configuration

Creates a DNS zone with custom SOA record fields.

{{tffile "examples/resources/gcore_dns_zone/with-soa.tf"}}

{{ .SchemaMarkdown | trimspace }}

{{ if .HasImport }}
## Import

Import is supported using the following syntax:

{{ codefile "shell" .ImportFile }}
{{ end }}
