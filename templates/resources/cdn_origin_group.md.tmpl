---
page_title: "{{ .Name }} {{ .Type }} - {{ .RenderedProviderName }}"
subcategory: ""
description: |-
  {{ .Description }}
---

# {{ .Name }} ({{ .Type }})

{{ .Description }}

## Example Usage

### Basic origin group with primary and backup

Create a CDN origin group with a primary origin and a backup origin for failover.

{{tffile "examples/resources/gcore_cdn_origin_group/basic.tf"}}

### S3 origin group with inline credentials

Create CDN origin groups that pull content from S3-compatible buckets. S3 credentials are write-only — they are sent to the API but never stored in Terraform state. Use `s3_credentials_version` to trigger credential re-sends.

{{tffile "examples/resources/gcore_cdn_origin_group/s3-auth.tf"}}

### Mixed origin group with host and S3 origins

Create a CDN origin group that combines host origins with S3 origins for failover.

{{tffile "examples/resources/gcore_cdn_origin_group/mixed.tf"}}

{{ .SchemaMarkdown | trimspace }}

{{ if .HasImport }}
## Import

Import is supported using the following syntax:

{{ codefile "shell" .ImportFile }}
{{ end }}
