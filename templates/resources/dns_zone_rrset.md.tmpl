---
page_title: "{{ .Name }} {{ .Type }} - {{ .ProviderName }}"
subcategory: ""
description: |-
  {{ .Description }}
---

# {{ .Name }} ({{ .Type }})

{{ .Description }}

## Example Usage

### A record

Creates an A record set with multiple IP addresses.

{{tffile "examples/resources/gcore_dns_zone_rrset/a-record.tf"}}

### MX record

Creates an MX record for mail routing.

{{tffile "examples/resources/gcore_dns_zone_rrset/mx-record.tf"}}

### TXT record with GeoDNS picker

Creates a TXT record on a subdomain with a GeoDNS picker for location-based responses.

{{tffile "examples/resources/gcore_dns_zone_rrset/txt-record.tf"}}

{{ .SchemaMarkdown | trimspace }}

{{ if .HasImport }}
## Import

Import is supported using the following syntax:

{{ codefile "shell" .ImportFile }}
{{ end }}
