---
page_title: "{{ .Name }} {{ .Type }} - {{ .RenderedProviderName }}"
subcategory: ""
description: |-
  {{ .Description }}
---

# {{ .Name }} ({{ .Type }})

{{ .Description }}

## Example Usage

### Boot volume from image

Creates a boot volume from an OS image.

{{tffile "examples/resources/gcore_cloud_volume/boot-volume.tf"}}

### Standalone data volume

Creates a blank data volume for additional storage.

{{tffile "examples/resources/gcore_cloud_volume/data-volume.tf"}}

### Volume restored from a snapshot

Restores a volume from a snapshot. The size comes from the snapshot, so it does
not have to be declared.

{{tffile "examples/resources/gcore_cloud_volume/from-snapshot.tf"}}

-> **`size` is required for every source except `snapshot`.** A volume created
with `source = "new-volume"` or `source = "image"` must declare a `size`; only a
snapshot restore derives it. Once the volume exists the size is tracked in state,
so removing `size` from the configuration is a no-op — set a larger value to
resize.

{{ .SchemaMarkdown | trimspace }}

{{ if .HasImport }}
## Import

Import is supported using the following syntax:

{{ codefile "shell" .ImportFile }}

~> **One reconciliation apply is needed after import.** The API does not return
the create-only fields `source`, `type_name`, `image_id`, `snapshot_id`,
`attachment_tag`, `instance_id_to_attach_to` or `lifecycle_policy_ids` in `GET`
responses, so they are absent from state immediately after import. The first
`terraform plan` therefore shows an update-in-place that copies these values
from your configuration into state. The volume is **not** replaced and no
infrastructure changes occur; subsequent plans are clean.
{{ end }}
