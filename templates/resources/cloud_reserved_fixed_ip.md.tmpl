---
page_title: "{{ .Name }} {{ .Type }} - {{ .RenderedProviderName }}"
subcategory: ""
description: |-
  {{ .Description }}
---

# {{ .Name }} ({{ .Type }})

{{ .Description }}

## Example Usage

### Reserve an external IP address

Reserves a public (external) IP address.

{{tffile "examples/resources/gcore_cloud_reserved_fixed_ip/external.tf"}}

### Reserve a private IP in any subnet

Reserves a private IP in any available subnet of a network.

{{tffile "examples/resources/gcore_cloud_reserved_fixed_ip/any-subnet.tf"}}

### Reserve a private IP in a specific subnet

Reserves a private IP in a specific subnet of a network.

{{tffile "examples/resources/gcore_cloud_reserved_fixed_ip/subnet.tf"}}

### Reserve a specific IP address

Reserves a specific IP address in a subnet.

{{tffile "examples/resources/gcore_cloud_reserved_fixed_ip/ip-address.tf"}}

### Reserve an existing port

Reserves an existing port, such as a load balancer VIP port.

{{tffile "examples/resources/gcore_cloud_reserved_fixed_ip/port.tf"}}

{{ .SchemaMarkdown | trimspace }}

{{ if .HasImport }}
## Import

Import is supported using the following syntax:

{{ codefile "shell" .ImportFile }}

~> **Import behavior varies by `type`:**

- **`external`** — Import produces a clean plan with no diffs.
- **`subnet`** — Import produces a clean plan with no diffs.
- **`any_subnet`** — The API cannot distinguish `subnet` from `any_subnet`, so the provider infers `type = "subnet"` during import. The first `terraform apply` after import reconciles the `type` value. No infrastructure changes occur and subsequent plans are clean.
- **`ip_address`** — This type cannot be inferred from the API response. After import, run `terraform apply` to reconcile the state.
- **`port`** — This type cannot be inferred from the API response. After import, run `terraform apply` to reconcile the state.
{{ end }}
