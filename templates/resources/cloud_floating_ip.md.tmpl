---
page_title: "{{ .Name }} {{ .Type }} - {{ .RenderedProviderName }}"
subcategory: ""
description: |-
  {{ .Description }}
---

# {{ .Name }} ({{ .Type }})

{{ .Description }}

## Example Usage

### Unattached floating IP

Allocate a floating IP address without attaching it to any resource.

{{tffile "examples/resources/gcore_cloud_floating_ip/basic.tf"}}

### Attach to an instance port

Allocate a floating IP and immediately attach it to an existing instance port.

{{tffile "examples/resources/gcore_cloud_floating_ip/attached.tf"}}

{{ .SchemaMarkdown | trimspace }}

{{ if .HasImport }}
## Import

Import is supported using the following syntax:

{{ codefile "shell" .ImportFile }}
{{ end }}
