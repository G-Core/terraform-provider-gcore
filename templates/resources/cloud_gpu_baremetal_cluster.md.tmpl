---
page_title: "{{ .Name }} {{ .Type }} - {{ .ProviderName }}"
subcategory: ""
description: |-
  {{ .Description }}
---

# {{ .Name }} ({{ .Type }})

{{ .Description }}

## Example Usage

### Prerequisite

{{tffile "examples/resources/gcore_cloud_gpu_baremetal_cluster/main.tf"}}

### Creating GPU cluster with one public interface and tags

{{tffile "examples/resources/gcore_cloud_gpu_baremetal_cluster/one-interface-with-tags.tf"}}

### Creating GPU cluster with two interfaces

This example demonstrates how to create a baremetal GPU cluster with two network interfaces: one public and one private.

{{tffile "examples/resources/gcore_cloud_gpu_baremetal_cluster/two-interface.tf"}}

### Creating GPU cluster with VAST file share

This example demonstrates how to create a baremetal GPU cluster with VAST file share manual mount.

{{tffile "examples/resources/gcore_cloud_gpu_baremetal_cluster/gpu-with-vast-manual-mount.tf"}}

{{ .SchemaMarkdown | trimspace }}

{{ if .HasImport }}
## Import

Import is supported using the following syntax:

{{ codefile "shell" .ImportFile }}
{{ end }}
